language: java

jdk:
  - oraclejdk7
  
script:
  - cd build; mvn install
  

# whitelist
branches:
  only:
    - master

# send coverage data
after_success:
  - mvn clean test jacoco:report coveralls:report